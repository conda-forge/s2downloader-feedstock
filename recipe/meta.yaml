{% set name = "s2downloader" %}
{% set version = "1.4.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/s2downloader-{{ version }}.tar.gz
  sha256: da628828e4c8e3d26fcfdcb735f86627dae6598b63547f3a2db2ab2b069fbd9a

build:
  entry_points:
    - s2downloader=s2downloader.s2downloader_cli:main
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python >=3.7
    - pytest-runner
    - pip
  run:
    - python >=3.7
    - gdal
    - affine
    - pyproj
    - numpy
    - matplotlib-base
    - geojson
    - rasterio
    - geopy
    - pandas
    - geopandas >=0.11
    - shapely
    - rtree
    - python-dateutil
    - pystac
    - pystac-client
    - pydantic
    - geojson-pydantic

test:
  imports:
    - s2downloader
  commands:
    - pip check
    - s2downloader --help
  requires:
    - pip

about:
  home: https://git.gfz-potsdam.de/fernlab/products/data-portal/s2downloader
  summary: Downloader for Sentinel-2 from aws.
  license: MPL-2.0
  license_file: LICENSE

extra:
  recipe-maintainers:
    - romulogoncalves
    - DaRa42
